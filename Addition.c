#include<stdio.h>
#include<conio.h>
int main()
{
    int c,a=10,b=24;
    if (a>5)
    {
        c=a+b;
        printf("Addition is: %d",c);
    }
    else {
            printf("we can not do addition");
    }
     
    return 0;
}